#include "hsp3utf.as"
#include "./modules/inimodule2_utf.as"

sdim dir,3

    dir(0) = "Ç†Ç†Ç†", "Ç¢Ç¢7Ç¢", "Ç§Ç§Ç§"
    //delete (dir_exe+"\\test.ini")
    SetIni (dir_exe+"\\testa.ini")
    WriteIni "test", "dirArr", dir(1)
    GetIni "test", "dirArr", pants, "\"Ç†Ç†Ç†\""

    repeat length(pants)
        mes ""+cnt+"çsñ⁄ "+pants(cnt)
    loop
    stop


/*
#const CB_ADDSTRING     0x0143

#define CBS_DROPDOWN    0x0002

winobj "ComboBox", "", 0, $50000000|CBS_DROPDOWN, Scale(95), Scale(22), hwnd, 0
FirstFleetNameCBh = objinfo(stat, 2)
sendmsg FirstFleetNameCBh, WM_SETFONT, hFontMain, 1
sendmsg FirstFleetNameCBh, CB_ADDSTRING, 0, FirstFleetName

objsize Scale(18), ComBoxH
pos window2x + Scale(140), mesy2
button gosub "+", *exit
objsize Scale(17), ComBoxH
pos window2x + Scale(158), mesy2
button gosub "-", *exit
mesy2+=ComBoxH
*/
